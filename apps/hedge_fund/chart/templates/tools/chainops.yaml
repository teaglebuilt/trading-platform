---
apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: chainops-mcp
  namespace: ai
spec:
  endpoint: "http://chainops.ai.svc.cluster.local" # small FastAPI/Foundry node
  auth:
    secretRef: chainops-keys
  tools:
    - name: push_nav
    - name: mint
    - name: burn
