[project]
name = "risk"
version = "0.1.0"
description = "Risk management rules for trading system"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.8"]

[tool.uv.sources]
common = { workspace = true }
